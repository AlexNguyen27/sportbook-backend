overwrite: true
schema: "http://127.0.0.1:9000/graphql"
generates:
  src/types/graphql.type.ts:
    config:
      useIndexSignature: true
    plugins:
      - "typescript"
      - "typescript-resolvers"
